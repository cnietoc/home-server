#!/usr/bin/env bash
# ============================================
# commands/deploy/up — Iniciar/deploy stacks
# ============================================
# Estado: 🚧 WIP (Work In Progress)
# ============================================

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"

source "$PROJECT_ROOT/lib/logs.sh"
source "$PROJECT_ROOT/lib/utils.sh"

# ============================================
# MAIN
# ============================================

main() {
    local cmd_prefix=$(utils::get_command_prefix "${BASH_SOURCE[0]}")

    logs::warn "🚧 Este comando está en desarrollo (WIP)"
    echo ""
    logs::info "📋 Comando: ${cmd_prefix}"
    logs::info "📝 Descripción: Iniciar/deploy stacks"
    echo ""
    logs::info "💡 Mientras tanto, puedes usar el script legacy:"
    logs::info "   ./scripts/deploy.sh [stack]"
    echo ""
    log_error "⏳ Implementación pendiente"
    echo ""
}

main "$@"
