name: ${STACK_PREFIX}

networks:
  hms-network:
    external: true

x-hms:
  description: "Servicios de infraestructura base"
  public_ports:
    - { port: 19998, protocol: tcp, description: "HMS test port" }

services:
  traefik:
    image: traefik:latest
    container_name: ${STACK_PREFIX}-traefik
    restart: unless-stopped
    networks:
      - hms-network
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ${STACK_DIR}/traefik:/etc/traefik:ro
      - ${STACK_DATA}/traefik/certs:/certs
      - ${STACK_DATA}/traefik/logs:/logs
    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
      - CLOUDFLARE_EMAIL=${CLOUDFLARE_EMAIL}
      - CLOUDFLARE_DNS_API_TOKEN=${CLOUDFLARE_DNS_API_TOKEN}
    command:
      # API y Dashboard
      - --api.dashboard=true
      - --api.insecure=true

      # Entrypoints
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443

      # Providers
      - --providers.docker=true
      - --providers.docker.network=proxy
      - --providers.docker.exposedbydefault=false
      - --providers.file.directory=/etc/traefik/dynamic
      - --providers.file.watch=true

      # Certificados SSL con Let's Encrypt
      - --certificatesresolvers.cloudflare.acme.dnschallenge=true
      - --certificatesresolvers.cloudflare.acme.dnschallenge.provider=cloudflare
      - --certificatesresolvers.cloudflare.acme.storage=/certs/acme.json
      - --certificatesresolvers.cloudflare.acme.dnschallenge.resolvers=1.1.1.1:53,8.8.8.8:53

      # Logs
      - --log.level=INFO
      - --log.filepath=/logs/traefik.log
      - --accesslog=true
      - --accesslog.filepath=/logs/access.log

      # Redirect HTTP to HTTPS
      - --entrypoints.web.http.redirections.entrypoint.to=websecure
      - --entrypoints.web.http.redirections.entrypoint.scheme=https
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=hms-network"
      - "traefik.http.routers.dashboard.rule=Host(`traefik.${DOMAIN}`)"
      - "traefik.http.routers.dashboard.tls=true"
      - "traefik.http.routers.dashboard.tls.certresolver=cloudflare"
      - "traefik.http.routers.dashboard.service=api@internal"
      - "traefik.http.routers.dashboard.middlewares=tinyauth@docker"
      - "hms.description=🔀 Traefik Dashboard"

  tinyauth:
    image: ghcr.io/steveiliop56/tinyauth:latest
    container_name: ${STACK_PREFIX}-tinyauth
    restart: unless-stopped
    networks:
      - hms-network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ${STACK_DATA}/tinyauth:/data
    environment:
      - TZ=${TZ}
      - PUID=${PUID}
      - PGID=${PGID}
      - TINYAUTH_APPURL=https://auth.${DOMAIN}
      - TINYAUTH_OAUTH_PROVIDERS_GOOGLE_CLIENTID=${AUTH_GOOGLE_CLIENT_ID}
      - TINYAUTH_OAUTH_PROVIDERS_GOOGLE_CLIENTSECRET=${AUTH_GOOGLE_CLIENT_SECRET}
      - TINYAUTH_OAUTH_WHITELIST=${AUTH_OAUTH_WHITELIST}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=hms-network"
      - "traefik.http.routers.tinyauth.rule=Host(`auth.${DOMAIN}`)"
      - "traefik.http.routers.tinyauth.tls=true"
      - "traefik.http.routers.tinyauth.tls.certresolver=cloudflare"
      - "traefik.http.middlewares.tinyauth.forwardauth.address=http://tinyauth:3000/api/auth/traefik"
      - "traefik.http.middlewares.tinyauth.forwardauth.maxResponseBodySize=1048576"
      - "hms.description=🔐 Panel de Autenticación"

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: ${STACK_PREFIX}-cadvisor
    restart: unless-stopped
    networks:
      - hms-network
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /dev/disk:/dev/disk:ro
    devices:
      - /dev/kmsg
    privileged: true
    command:
      - --housekeeping_interval=10s
      - --global_housekeeping_interval=60s
      - --docker_only=true
      - --disable_metrics=advtcp,cpu_topology,cpuset,hugetlb,memory_numa,process,referenced_memory,resctrl,sched,tcp,udp
      - --allow_dynamic_housekeeping=true
    environment:
      - TZ=${TZ}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=hms-network"
      - "traefik.http.routers.cadvisor.rule=Host(`monitor.${DOMAIN}`)"
      - "traefik.http.routers.cadvisor.tls=true"
      - "traefik.http.routers.cadvisor.tls.certresolver=cloudflare"
      - "traefik.http.routers.cadvisor.service=cadvisor"
      - "traefik.http.services.cadvisor.loadbalancer.server.port=8080"
      - "traefik.http.routers.cadvisor.middlewares=tinyauth@docker"
      - "hms.description=📊 Monitor de contenedores Docker"

